# Gherkin step definitions

- `step_impl.rs` implements executable Given/When/Then mappings.
- `tests/gherkin_runner.rs` parses `.feature` files and dispatches steps.
- Scope: API/UI contract checks for mailbox/newsletter/draft critical paths.
